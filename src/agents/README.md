# Specialist role lifecycle

Shared installation, discovery and ownership checks for model-neutral specialist definitions. Prefer suitable existing project/personal roles, enforce the required model tier, preserve user modifications and remove only unchanged managed profiles. Legacy single-role installation state is migrated at the hook installation boundary.
