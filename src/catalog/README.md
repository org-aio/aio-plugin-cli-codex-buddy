# 模型目录

标准模型列表唯一决定成员、顺序和显示名称；旧目录、内置目录、隐藏模型或网关元数据均不补充 API 列表之外的 ID。元数据优先级为当前客户端内置模型、已有自定义模型、可选网关清单、保守通用默认值。不靠模型名称推断推理、视觉或工具调用能力。

输入能力例外：网关清单明确提供合法的 `input_modalities` 时，覆盖已有目录及内置值，并同步 `supports_image_detail_original`。这让网关视觉辅助的启用和撤回都能在下一次同步生效；其他字段仍遵守上述优先级。客户端需要重新加载目录后才能使用更新后的能力。
