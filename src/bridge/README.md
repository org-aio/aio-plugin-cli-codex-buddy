# App Server 标准输入输出代理

仅改写开始新一轮的 `turn/start.model` / `effort`，同步处理桌面实验性 collaborationMode。
工具审批、请求 ID、服务器反向请求和其他消息透传；活动轮次的 steering 不改模型。
通过协议原生 `warning` 通知显示自动选择，服务器接受后显示“已启用”；这表示接受了请求，不保证供应商没有内部别名映射。
模型列表失败时保留原请求，公开报告分流未生效；不重试有潜在副作用的任务。
状态文件仅包含模型、等级、原因、时间及任务/轮次 ID，不保存提示词或凭据。
