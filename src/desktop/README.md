# macOS 桌面接入

使用本机桌面包中已验证存在的 `CODEX_CLI_PATH` 环境变量接入，不修改应用签名或内置二进制。
setup 安装独立 shim，并用 launchctl 为下次启动设置路径，登录 LaunchAgent 保持设置。
现有桌面进程需退出重开；不会主动终止当前任务。

这是版本相关的桌面内部接入点，应用更新后需重新验证。使用桌面随附 Codex 避免 CLI 协议版本错配。
uninstall 恢复先前环境值，停止登录项并禁用策略；保留 shim 防止当前桌面进程启动工具失败。
